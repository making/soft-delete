<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="">
  <link href="/style.css" rel="stylesheet"/>
  <title>Promote to Admin</title>
</head>
<body>
<div class="container">
  <h3 class="header">Promote to Admin</h3>
  
  <div class="account-section">
    <div class="account-field">
      <label class="field-label">User ID</label>
      <div class="field-value">{{user.userId}}</div>
    </div>
    <div class="account-field">
      <label class="field-label">Username</label>
      <div class="field-value">{{user.userProfile.username}}</div>
    </div>
    <div class="account-field">
      <label class="field-label">Display Name</label>
      <div class="field-value">{{user.userProfile.displayName}}</div>
    </div>
    <div class="account-field">
      <label class="field-label">Email</label>
      <div class="field-value">{{user.primaryEmail}}</div>
    </div>
  </div>
  
  <p class="message" style="color: #dc3545; margin-top: 20px;">
    Are you sure you want to promote this user to admin? This action cannot be undone.
  </p>
  
  <form action="/admin/users/{{user.userId}}/promote" method="post" style="text-align: center;">
    {{#_csrf}}
    <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    {{/_csrf}}
    
    <button class="styled-button admin" type="submit" style="margin-bottom: 10px;">
      Yes, Promote to Admin
    </button>
  </form>
  <form action="/admin/users/{{user.userId}}/promote" method="post" style="text-align: center;">
    {{#_csrf}}
      <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    {{/_csrf}}
    <button class="styled-button secondary" type="submit" name="cancel">Cancel</button>
  </form>
</div>
</body>
</html>